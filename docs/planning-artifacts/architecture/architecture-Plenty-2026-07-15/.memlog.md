---
scope: Plenty full app architecture
purpose: Build-substrate for Sprints 6-9 development
altitude: feature
updated: 2026-07-16T01:26
---

- (version) Expo SDK ~55.0.0, React Native 0.83.6, React 19.2.0 (current)
- (version) expo-notifications ~55.0.24 — local repeating notifications, no push server
- (decision) Screen-based RN app with no global state library — screens directly import utils (storage, notifications, etc). Theme via React Context.
- (decision) AD-6: Data layer is AsyncStorage key-value — no SQLite, no backend. Three namespaces: @plenty_logs, @plenty_settings, @plenty_monthly_cache.
- (decision) AD-4: Home screen is the single Source of Truth for notification state (start/stop, interval). Notification scheduling/response logic lives in utils/notifications.js
- (decision) AD-3: Theme via React Context + light/dark color tokens in constants/colors.js. ThemePreference persisted in AsyncStorage
- (decision) Sprint 7 adds react-native-reanimated, react-native-svg, expo-haptics as native deps
- (decision) AD-7: Android widget via Kotlin Expo module (modules/plenty-widget). Widget bridge in utils/widget.js. iOS WidgetKit planned for Sprint 9.
- (decision) AD-9: Health Connect to be purged before Play Store submission — simplifies Data Safety declaration
- (decision) AD-10: Navigation via @react-navigation/bottom-tabs — 4 fixed tabs (Home, Log, Achievements, Settings). No deep navigation hierarchy.
- (decision) AD-3 tightened: all color values must be theme tokens with light/dark variants in colors.js — standalone named constants violating theme bypass are banned
- (decision) AD-4/AD-5 broadened: notifications.js owns complete schedule via single gate; any new scheduling function must interact with dedup guard; Settings uses remindersActive flag
- (decision) AD-6 added key ownership: all @plenty_settings defaults register in single DEFAULT_SETTINGS in storage.js; unknown keys stripped on write
- (decision) AD-10 added sub-screen pattern (screens/sub/) for feature depth without exceeding 4-tab limit
- (decision) Added component behavioral contract and cross-util import convention to Consistency Conventions
- (decision) Stack table: added @react-navigation/native, expo-dev-client; annotated health deps as TO BE REMOVED
- (decision) Deferred: guarded Zustand language with stores/ directory pattern; added testing strategy entry
- (event) spine finalized — reviewer findings resolved: AD-3/4/5/6/10 tightened, stack/seed/conventions corrected, deferred entries added
