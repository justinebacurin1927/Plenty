# Team-level overrides for bmad-qa-generate-e2e-tests (committed)
# Override the skill defaults in customize.toml here.
# Merge rules: scalars override, arrays append (unless keyed by code/id).
# See the skill's own customize.toml for all available fields.

